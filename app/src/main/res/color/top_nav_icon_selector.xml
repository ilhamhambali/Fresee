<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Warna saat item navigasi dipilih (aktif) -->
    <item android:state_checked="true" android:color="@color/green_leaf" />

    <!-- Warna default (saat tidak dipilih) -->
    <item android:color="@color/white_dimmed" />

</selector>